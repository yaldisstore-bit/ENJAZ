import {useEffect,useLayoutEffect,useState} from 'react';import {createPortal} from 'react-dom';import type {DocumentFactoryGateway} from '../../features/documents/documentFactoryCommands.ts';import type {DocumentIntelligenceGateway} from '../../features/documents/documentIntelligenceCommands.ts';import type {DocumentVaultGateway} from '../../features/documents/documentVaultCommands.ts';import type {EngagementContractGateway} from '../../features/engagements/engagementContractCommands.ts';import type {DocumentFactoryFactory,DocumentIntelligenceFactory,DocumentVaultFactory,EngagementContractFactory} from '../runtime/UiR2ProductionRoot.tsx';import {ConnectedDocumentVault} from './ConnectedDocumentVault.tsx';import {EngagementContractPanel} from './EngagementContractPanel.tsx';
const SHELL='.r2-shell[data-r2-runtime-mode="live"][data-destination]',PREVIEW='[data-records-stage="R2.0-6"][data-records-domain="documents"]';
export function LiveDocumentVaultPortal({factory,intelligenceFactory,documentFactoryFactory,engagementContractFactory,workspace}:{factory:DocumentVaultFactory;intelligenceFactory?:DocumentIntelligenceFactory|undefined;documentFactoryFactory?:DocumentFactoryFactory|undefined;engagementContractFactory?:EngagementContractFactory|undefined;workspace:Promise<string|null>}){const[target,setTarget]=useState<HTMLElement|null>(null),[active,setActive]=useState(false),[gateway,setGateway]=useState<DocumentVaultGateway|null>(null),[intelligence,setIntelligence]=useState<DocumentIntelligenceGateway|null>(null),[documentFactory,setDocumentFactory]=useState<DocumentFactoryGateway|null>(null),[contracts,setContracts]=useState<EngagementContractGateway|null>(null),[workspaceId,setWorkspaceId]=useState<string|null>(null);useLayoutEffect(()=>{const shell=document.querySelector<HTMLElement>(SHELL),main=document.getElementById('r2-main');if(!shell||!main)return;setTarget(main);const sync=()=>setActive(shell.dataset.destination==='documents');sync();const o=new MutationObserver(sync);o.observe(shell,{attributes:true,attributeFilter:['data-destination']});return()=>o.disconnect()},[]);useLayoutEffect(()=>{if(!target)return;const preview=target.querySelector<HTMLElement>(PREVIEW);if(preview)preview.hidden=active;return()=>{if(preview)preview.hidden=false}},[active,target]);useEffect(()=>{let live=true;void workspace.then(ws=>{if(live)setWorkspaceId(ws)});return()=>{live=false}},[workspace]);useEffect(()=>{if(!active||gateway)return;let live=true;void factory().then(g=>{if(live)setGateway(g)});return()=>{live=false}},[active,factory,gateway]);useEffect(()=>{if(!active||!intelligenceFactory||intelligence)return;let live=true;void intelligenceFactory().then(g=>{if(live)setIntelligence(g)}).catch(()=>undefined);return()=>{live=false}},[active,intelligenceFactory,intelligence]);useEffect(()=>{if(!active||!documentFactoryFactory||documentFactory)return;let live=true;void documentFactoryFactory().then(g=>{if(live)setDocumentFactory(g)}).catch(()=>undefined);return()=>{live=false}},[active,documentFactoryFactory,documentFactory]);useEffect(()=>{if(!active||!engagementContractFactory||contracts)return;let live=true;void engagementContractFactory().then(g=>{if(live)setContracts(g)}).catch(()=>undefined);return()=>{live=false}},[active,engagementContractFactory,contracts]);if(!active||!target||!gateway)return null;return createPortal(<><ConnectedDocumentVault gateway={gateway} workspace={workspace} {...(intelligence?{intelligenceGateway:intelligence}:{})} {...(documentFactory?{documentFactoryGateway:documentFactory}:{})}/>{contracts&&documentFactory&&workspaceId?<EngagementContractPanel gateway={contracts} documentFactoryGateway={documentFactory} workspaceId={workspaceId}/>:null}</>,target)}