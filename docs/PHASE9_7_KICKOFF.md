# Phase 9.7 — Intelligence Zero-Escape Gate Kickoff

**Status: IN PROGRESS**

Canonical base: `main` @ `bcb8e697fa7457e216036ae321b8eddac2050b1b`, the formally closed Phase 9.6 successor authorization.

Branch: `phase9-7-intelligence-zero-escape`

## Purpose

Phase 9.7 is **not a feature-delivery phase**. It is the destructive intelligence closure gate for the Phase-9 intelligence systems and their existing authority boundaries.

No new product authority, database table, browser-owned truth store, public write RPC, feature cut or budget increase is authorized by this phase. If destruction exposes a real defect, only the smallest root-cause repair plus permanent regression coverage is allowed.

## Systems under the gate

- **M2 — Corporate Governance & Ownership Engine** — individual Phase-9 Zero-Escape evidence; global status remains `CLOSURE_CANDIDATE` until the independent M-system closure law is fully satisfied.
- **M8 — Regulatory / Knowledge Base Engine** — Phase-9 portion only; M8 remains `ACTIVE` because Phase 12 is still an open anchor.
- **M13 — Business Intelligence & Forecasting Center** — Phase-9 portion only; M13 remains `ACTIVE` because Phase 15 is still open.
- **M18 — Process Mining & Predictive Operations** — Phase-9 portion only; M18 remains `ACTIVE` because Phase 15 is still open.

## Destruction dimensions

The gate must attack all roadmap-required intelligence failure classes:

1. **conflicting/stale signals** — contradictory ownership/effective history, future/stale BI/process evidence and inconsistent lineage must fail closed;
2. **no-data states** — absence of provenance/history/source version must never fabricate a KPI, legal citation, process prediction or ownership truth;
3. **high-volume datasets** — exact ownership, regulatory lineage, BI trends and process predictions must remain deterministic under dense history;
4. **invalid ownership** — impossible totals, overlapping effective periods and incomplete reconciliations remain rejected;
5. **regulatory version conflicts** — duplicate/forked/overlapping authoritative versions and fabricated citations remain rejected;
6. **model drift** — forecasts/predictions must be recomputed from supplied current evidence, never remain stale or silently become authoritative;
7. **prediction uncertainty** — insufficient/tied samples must expose uncertainty and null prediction/projection rather than fabricate confidence.

## Existing authority that must remain frozen

### M2
- exact micro-percent ownership arithmetic;
- append/effective-dated history;
- no shadow company/person truth;
- invalid or conflicting ownership fails closed.

### M8
- official source/version/provenance tuple is authoritative;
- AI/editorial output remains `authoritative=false`;
- version lineage is deterministic and non-overlapping;
- no legal citation without a matching source/version/provenance tuple.

### M13
- `READ_ONLY_DERIVED` BI only;
- source provenance required;
- finance remains exact bigint cents;
- forecasts remain directional/non-authoritative;
- unsupported ratio/duration linear run-rate fails closed.

### M18
- process intelligence remains read-only derived from source-owned history;
- equal-time evidence is partial order, not fabricated strict order;
- prediction methods remain empirical and directional/non-authoritative;
- ties/insufficient samples expose uncertainty;
- no synthetic process history or shadow process persistence.

## Gate layers

Phase 9.7 follows `ZERO_ESCAPE_V1` and cannot close from branch CI alone.

Required before formal closure:

- exact-head deterministic destruction tests;
- full Phase 9.3–9.6 regression;
- DB/RLS/roadmap/secrets/TypeScript/budget gates;
- authenticated Real Cloud verification for persistent source authority and RLS where applicable;
- Real Chromium at **1280 / 430 / 390 / 360 / 320** across M2/M8/M13/M18 surfaces;
- Pages `/live/` and deployed-live critical paths;
- exact merged SHA post-merge recertification;
- zero Critical, High or functional blockers.

## Successor lock

**Phase 10.1 — Document Vault remains LOCKED.**

It becomes authorized only after Phase 9.7 is formally closed on exact `main`, all required Real Cloud / Real Browser / deployed-live evidence is complete, and the required workflow ledger has zero failure / queued / in-progress runs.
