# Phase 14.1 A2 — J01–J08 joined authenticated cloud journey (2026-09-20)

**Exact executable SHA:** `fce600b40fb318c3c7a29e9a4a3eb110bba3103c`. [Run #35538625980](https://github.com/yaldisstore-bit/ENJAZ/actions/runs/35538625980) completed **SUCCESS**. Its isolated-target preflight, dependency and syntax checks, single real-user hosted J01–J08 sequence, exact-scope evidence guard and artifact upload succeeded. The workflow deliberately retains `completeElevenDomainA2 === false` and `phase14_1Closed === false`.

The same disposable company, transaction, procedure, field visit, follow-up, payment with one reversal, and generated approved private PDF passed through the seven predecessor domains. J08 then created and separately signed in a third **real client** account, whose personal workspace and Auth user were distinct from the two staff-fixture users. The client was not given workspace staff or organization workforce membership in the target workspace. The client discovered and activated an explicit invitation, and received no company/transaction/document/receipt projection before its permissions were granted. The one-target-scoped company/transaction grants and individual document/receipt resource shares were set up via **test-admin fixture scaffolding**, not presented as an end-user grant-management certificate. With an independent signed-in JWT, the client observed the explicitly shared source records only, while the staff Data API remained inaccessible. Revoking the scoped grants removed the projections. Cross-workspace invitation activation and uninvited client access were rejected.

After the completed run, an independent READ-ONLY Supabase lab query confirmed **zero** Auth users and sessions, workspaces, companies, transactions, procedures/field/followups, payments/reversals, document drafts/PDF jobs/documents/versions, client portal principals/grants/resource shares and storage objects. No production data was mutated. The lab was `nqhgaukutkyvfumbtbtg`, not production `juzxriirhkuzviwnhkbd`.

**Not certified:** separately published web portal / physical Android UI, staff-governed creation of portal invitations/grants/shares, complete same-workspace role matrix, J09–J11 in the same journey, exact-final-HEAD comprehensive CI and exact-main deployed-live recertification. PR #225 remains DRAFT, 14.1 IN_PROGRESS, 14.2 LOCKED. Later documentation or code commits do not inherit this exact executable source's certificate.
