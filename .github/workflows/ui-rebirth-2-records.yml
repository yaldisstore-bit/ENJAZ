name: ENJAZ R2.0-6 — Records & Relationships Gate

on:
  push:
    branches:
      - 'r2-0-6-*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  records-gate:
    name: R2.0-6 / Entity-first Records + Real Browser
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Enforce Phase 5.5 freeze
        run: node scripts/ui-rebirth-2-freeze-audit.mjs
      - name: Enforce locked palette
        run: node scripts/ui-rebirth-2-palette-audit.mjs
      - name: Preserve Golden + Core Work
        run: |
          node scripts/ui-rebirth-2-golden-visual-contract-audit.mjs
          node scripts/ui-rebirth-2-golden-specimen-audit.mjs
          node scripts/ui-rebirth-2-core-work-audit.mjs
      - name: Enforce R2.0-6 Records contract
        run: node scripts/ui-rebirth-2-records-audit.mjs
      - name: Functional regression
        run: npm run test:functional
      - name: TypeScript
        run: npm run typecheck
      - name: Build isolated R2.0-6 preview
        run: npx vite build --config vite.r2-preview.config.ts --base=/
      - name: Enforce isolated preview budget
        run: node scripts/ui-rebirth-2-preview-budget-audit.mjs
      - name: Install pinned Chromium tooling
        run: |
          npm install --no-save --package-lock=false @playwright/test@1.55.0 playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start isolated R2.0-6 preview
        run: |
          nohup npx vite preview --config vite.r2-preview.config.ts --host 127.0.0.1 --port 4174 > /tmp/enjaz-r2-records-preview.log 2>&1 &
          for i in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4174/r2-preview.html >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/enjaz-r2-records-preview.log
          exit 1
      - name: Real browser Records acceptance / 1280 + 430 + 390 + 360 + 320
        env:
          R2_RECORDS_BASE_URL: http://127.0.0.1:4174/
          R2_RECORDS_ARTIFACT_DIR: artifacts/r2-records
        run: npx playwright test tests-external/r2-records.spec.cjs --reporter=line --workers=1
      - name: Upload R2.0-6 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: enjaz-r2-records-evidence-${{ github.sha }}
          path: |
            artifacts/r2-records/
            test-results/
            /tmp/enjaz-r2-records-preview.log
          if-no-files-found: ignore
          retention-days: 14
