name: ENJAZ R2.0-7 — Operational Intelligence Gate

on:
  push:
    branches:
      - 'r2-0-7-*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  operational-intelligence-gate:
    name: R2.0-7 / Task-specific Operational Intelligence + Real Browser
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Enforce Phase 5.5 freeze
        run: node scripts/ui-rebirth-2-freeze-audit.mjs
      - name: Enforce locked palette
        run: node scripts/ui-rebirth-2-palette-audit.mjs
      - name: Preserve hard Rebirth acceptance
        run: node scripts/ui-rebirth-2-acceptance-audit.mjs
      - name: Preserve Golden + Core Work + Records
        run: |
          node scripts/ui-rebirth-2-golden-visual-contract-audit.mjs
          node scripts/ui-rebirth-2-golden-specimen-audit.mjs
          node scripts/ui-rebirth-2-core-work-audit.mjs
          node scripts/ui-rebirth-2-records-audit.mjs
      - name: Enforce R2.0-7 Operational Intelligence contract
        run: node scripts/ui-rebirth-2-operational-intelligence-audit.mjs
      - name: Functional regression
        run: npm run test:functional
      - name: TypeScript
        run: npm run typecheck
      - name: Build isolated R2.0-7 preview
        run: npx vite build --config vite.r2-preview.config.ts --base=/
      - name: Enforce isolated preview budget
        run: node scripts/ui-rebirth-2-preview-budget-audit.mjs
      - name: Install pinned Chromium tooling
        run: |
          npm install --no-save --package-lock=false @playwright/test@1.55.0 playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start isolated R2.0-7 preview
        run: |
          nohup npx vite preview --config vite.r2-preview.config.ts --host 127.0.0.1 --port 4174 > /tmp/enjaz-r2-operational-preview.log 2>&1 &
          for i in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4174/r2-preview.html >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/enjaz-r2-operational-preview.log
          exit 1
      - name: Real browser Operational Intelligence acceptance / 1280 + 430 + 390 + 360 + 320
        env:
          R2_OPERATIONAL_BASE_URL: http://127.0.0.1:4174/
          R2_OPERATIONAL_ARTIFACT_DIR: artifacts/r2-operational
        run: npx playwright test tests-external/r2-operational-intelligence.spec.cjs --reporter=line --workers=1
      - name: Upload R2.0-7 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: enjaz-r2-operational-evidence-${{ github.sha }}
          path: |
            artifacts/r2-operational/
            test-results/
            /tmp/enjaz-r2-operational-preview.log
          if-no-files-found: ignore
          retention-days: 14
