import {createClient} from '@supabase/supabase-js';
import {mkdir,writeFile} from 'node:fs/promises';
import crypto from 'node:crypto';

const PROJECT_REF='juzxriirhkuzviwnhkbd',BUCKET='enjaz-documents-private',ARTIFACT_DIR='artifacts/phase10-3-real-cloud-render',EVIDENCE_PATH=`${ARTIFACT_DIR}/evidence.json`;
const env=(n)=>{const v=process.env[n]?.trim();if(!v)throw new Error(`Missing required environment variable: ${n}`);return v};
const url=env('SUPABASE_URL').replace(/\/$/,''),publishableKey=env('SUPABASE_PUBLISHABLE_KEY'),secretKey=env('SUPABASE_SECRET_KEY');
if(process.env.ENJAZ_REAL_CLOUD_CONFIRM!=='YES')throw new Error('ENJAZ_REAL_CLOUD_CONFIRM must equal YES');
if(!url.includes(PROJECT_REF))throw new Error('Refusing unexpected Supabase project');
if(secretKey.startsWith('sb_publishable_')||secretKey===publishableKey)throw new Error('SUPABASE_SECRET_KEY is not privileged');
const admin=createClient(url,secretKey,{auth:{autoRefreshToken:false,persistSession:false,detectSessionInUrl:false}}),userClient=()=>createClient(url,publishableKey,{auth:{autoRefreshToken:false,persistSession:false,detectSessionInUrl:false}});
const uuid=()=>crypto.randomUUID(),sleep=(ms)=>new Promise(r=>setTimeout(r,ms)),hash=(bytes)=>crypto.createHash('sha256').update(bytes).digest('hex');
const evidence={schema:'enjaz.phase10-3-real-cloud-render.v1',projectRef:PROJECT_REF,startedAt:new Date().toISOString(),completedAt:null,passed:false,checks:[],cleanup:[]};
let fatal=null,workspaceId=null,user=null,storagePath=null;const record=(name,detail=null)=>{evidence.checks.push({name,passed:true,...(detail?{detail}:{})});console.log(`PASS ${name}${detail?` — ${detail}`:''}`)},assert=(ok,name,detail=null)=>{if(!ok)throw new Error(`ASSERTION_FAILED:${name}${detail?`:${detail}`:''}`);record(name,detail)};
const transient=(e)=>{const status=Number(e?.status??e?.statusCode??0),message=String(e?.message??e);return status===429||status===500||status===502||status===503||status===504||/gateway timeout|temporar|network|fetch failed|socket|econnreset|etimedout/i.test(message)};
async function retryTransient(label,fn,attempts=4){let last;for(let i=0;i<attempts;i++){try{return await fn()}catch(e){last=e;if(!transient(e)||i===attempts-1)throw e;const delay=500*(2**i);console.warn(`TRANSIENT_RETRY ${label} attempt=${i+1} delayMs=${delay}`);await sleep(delay)}}throw last}
async function createUser(){const email=`enjaz-phase10-3-render-${Date.now()}-${uuid().slice(0,8)}@example.com`,password=`EnjAZ!${uuid()}Aa9`;const created=await retryTransient('admin_create_user',async()=>{const r=await admin.auth.admin.createUser({email,password,email_confirm:true,user_metadata:{enjaz_test_marker:'phase10_3_real_cloud_render'}});if(r.error||!r.data.user)throw r.error??new Error('Unable to create user');return r});const client=userClient(),signed=await retryTransient('password_sign_in',async()=>{const r=await client.auth.signInWithPassword({email,password});if(r.error||!r.data.session?.access_token)throw r.error??new Error('No test JWT');return r});return{id:created.data.user.id,email,password,client,token:signed.data.session.access_token}}
async function workspaceFor(userId){for(let i=0;i<24;i++){const {data,error}=await admin.from('workspaces').select('id').eq('owner_user_id',userId).limit(2);if(error){if(transient(error)){await sleep(250);continue}throw error}if(data?.length===1)return data[0].id;await sleep(250)}throw new Error('Workspace bootstrap timeout')}
async function edge(slug,body){const r=await fetch(`${url}/functions/v1/${slug}`,{method:'POST',headers:{apikey:publishableKey,Authorization:`Bearer ${user.token}`,'Content-Type':'application/json'},body:JSON.stringify(body)});return{status:r.status,ok:r.ok,data:await r.json().catch(()=>null)}}
async function rpc(name,args){const r=await user.client.rpc(name,args);if(r.error)throw r.error;return r.data}
async function cleanup(){if(storagePath){try{const {error}=await admin.storage.from(BUCKET).remove([storagePath]);if(error&&!/not found/i.test(error.message))throw error;evidence.cleanup.push({kind:'storage_object',id:storagePath,passed:true})}catch(e){evidence.cleanup.push({kind:'storage_object',id:storagePath,passed:false,error:String(e)})}}
 if(workspaceId){try{const {error}=await admin.from('workspaces').delete().eq('id',workspaceId);if(error)throw error;const {count,error:countError}=await admin.from('workspaces').select('id',{count:'exact',head:true}).eq('id',workspaceId);if(countError)throw countError;if(count!==0)throw new Error('workspace residue');evidence.cleanup.push({kind:'workspace',id:workspaceId,passed:true})}catch(e){evidence.cleanup.push({kind:'workspace',id:workspaceId,passed:false,error:String(e)})}}
 if(user){try{const {error}=await admin.auth.admin.deleteUser(user.id,false);if(error)throw error;evidence.cleanup.push({kind:'auth_user',id:user.id,passed:true})}catch(e){evidence.cleanup.push({kind:'auth_user',id:user.id,passed:false,error:String(e)})}}
}
async function persist(){evidence.completedAt=new Date().toISOString();evidence.passed=!fatal;const bad=evidence.cleanup.filter(x=>!x.passed);if(bad.length&&evidence.passed){evidence.passed=false;evidence.failure=`Cleanup failed: ${bad.length}`}else if(fatal)evidence.failure=fatal instanceof Error?fatal.message:String(fatal);await mkdir(ARTIFACT_DIR,{recursive:true});await writeFile(EVIDENCE_PATH,`${JSON.stringify(evidence,null,2)}\n`,'utf8')}

try{
 user=await createUser();workspaceId=await workspaceFor(user.id);record('disposable_authenticated_workspace_ready');
 const companyId=uuid(),templateId=uuid(),templateVersionId=uuid(),generationRequestId=uuid(),renderRequestId=uuid();
 const company={id:companyId,workspace_id:workspaceId,legal_name:'شركة إنجاز العربية للتجارة العامة محدودة المسؤولية',display_name:'إنجاز العربية',capital:100000000,address:'العراق - بغداد - اليرموك',registration_number:'ENJAZ-2026-103',legal_status:'محدودة المسؤولية'};
 const inserted=await admin.from('companies').insert(company);if(inserted.error)throw inserted.error;record('fixture_company_created');
 const body=['كتاب رسمي — إنجاز','الشركة / {{company_name}}','رقم التسجيل / {{registration_number}}','رأس المال / {{capital}}','العنوان / العراق - بغداد - اليرموك','الموضوع / اختبار التوليد العربي الرسمي'].concat(Array.from({length:95},(_,i)=>`البند ${i+1}: هذا نص عربي رسمي لاختبار اتجاه الكتابة وترابط الحروف وترقيم الصفحات في سنة 2026، ويجب أن يبقى مقروءاً ومنظماً عند الانتقال بين الصفحات.`)).join('\n');
 const tokenSchema={company_name:{source:'company',field:'legal_name',required:true},registration_number:{source:'company',field:'registration_number',required:true},capital:{source:'company',field:'capital',required:true}};
 await rpc('save_document_template_v1',{p_workspace_id:workspaceId,p_template_id:templateId,p_name:'اختبار الكتاب العربي الرسمي',p_kind:'official-letter',p_body_source:body,p_token_schema:tokenSchema,p_active:true});
 const version=await rpc('create_document_template_version_v1',{p_workspace_id:workspaceId,p_request_id:templateVersionId,p_template_id:templateId,p_body_source:body,p_token_schema:tokenSchema});assert(version?.versionId===templateVersionId,'immutable_template_version_created');
 const published=await rpc('publish_document_template_version_v1',{p_workspace_id:workspaceId,p_version_id:templateVersionId});assert(published?.state==='published','template_version_published');
 const generated=await rpc('generate_document_draft_v1',{p_workspace_id:workspaceId,p_request_id:generationRequestId,p_template_version_id:templateVersionId,p_title:'كتاب إنجاز العربي الرسمي',p_company_id:companyId,p_transaction_id:null,p_contact_id:null,p_ocr_analysis_id:null});const draftId=generated?.draftId;assert(typeof draftId==='string'&&generated?.status==='review_required','authoritative_arabic_draft_generated');
 const {data:draftBefore,error:draftErr}=await admin.from('document_drafts').select('compiled_content,status,content_checksum,fact_snapshot_checksum').eq('id',draftId).single();if(draftErr)throw draftErr;assert(draftBefore.compiled_content.includes(company.legal_name)&&draftBefore.compiled_content.includes('ENJAZ-2026-103'),'authoritative_company_facts_compiled');
 const approved=await rpc('review_document_draft_v1',{p_workspace_id:workspaceId,p_draft_id:draftId,p_decision:'approve',p_note:'Real Cloud Arabic PDF certificate'});assert(approved?.status==='approved','human_approval_recorded');
 const requested=await rpc('request_document_render_v1',{p_workspace_id:workspaceId,p_request_id:renderRequestId,p_draft_id:draftId});const jobId=requested?.jobId;assert(typeof jobId==='string'&&requested?.status==='queued','governed_render_requested');
 const rendered=await edge('enjaz-document-render',{workspaceId,jobId});assert(rendered.ok&&rendered.data?.status==='succeeded'&&rendered.data?.documentId&&rendered.data?.documentVersionId, 'deployed_renderer_returned_real_output',`HTTP ${rendered.status}`);
 const documentId=rendered.data.documentId,versionId=rendered.data.documentVersionId;
 const {data:job,error:jobErr}=await admin.from('pdf_jobs').select('status,source_draft_id,source_template_version_id,source_content_checksum,source_fact_snapshot_checksum,output_document_id,output_document_version_id,service_run_id').eq('id',jobId).single();if(jobErr)throw jobErr;assert(job.status==='succeeded'&&job.source_draft_id===draftId&&job.source_template_version_id===templateVersionId&&job.output_document_id===documentId&&job.output_document_version_id===versionId&&typeof job.service_run_id==='string','render_authority_persisted_exact_binding');
 const {data:doc,error:docErr}=await admin.from('documents').select('id,status,document_type,mime_type,size_bytes,checksum,storage_path,company_id').eq('id',documentId).single();if(docErr)throw docErr;const {data:ver,error:verErr}=await admin.from('document_versions').select('id,document_id,version_number,mime_type,size_bytes,checksum,storage_path').eq('id',versionId).single();if(verErr)throw verErr;storagePath=ver.storage_path;assert(doc.status==='ready'&&doc.document_type==='generated-official'&&doc.mime_type==='application/pdf'&&doc.company_id===companyId&&ver.document_id===documentId&&ver.version_number===1,'vault_authority_contains_generated_official_pdf');
 const downloaded=await admin.storage.from(BUCKET).download(storagePath);if(downloaded.error||!downloaded.data)throw downloaded.error??new Error('Generated PDF missing from private storage');const bytes=new Uint8Array(await downloaded.data.arrayBuffer()),checksum=hash(bytes),latin=Buffer.from(bytes).toString('latin1'),pageCount=(latin.match(/\/Type\s*\/Page\b/g)||[]).length;assert(Buffer.from(bytes.slice(0,5)).toString('ascii')==='%PDF-','real_output_has_pdf_signature');assert(bytes.byteLength===ver.size_bytes&&bytes.byteLength===doc.size_bytes&&bytes.byteLength>5000,'real_pdf_size_verified',String(bytes.byteLength));assert(checksum===ver.checksum&&checksum===doc.checksum&&checksum===rendered.data.checksum,'real_pdf_sha256_verified');assert(pageCount>=2,'rtl_arabic_multipage_pagination_verified',`${pageCount} pages`);
 const replay=await edge('enjaz-document-render',{workspaceId,jobId});assert(replay.ok&&replay.data?.wasDuplicate===true&&replay.data?.documentId===documentId&&replay.data?.documentVersionId===versionId,'renderer_idempotent_replay_same_output');const {count:generatedCount,error:countErr}=await admin.from('documents').select('id',{count:'exact',head:true}).eq('workspace_id',workspaceId).eq('document_type','generated-official');if(countErr)throw countErr;assert(generatedCount===1,'renderer_replay_created_no_duplicate_document');
 const finalized=await rpc('finalize_document_draft_v1',{p_workspace_id:workspaceId,p_draft_id:draftId,p_render_job_id:jobId});assert(finalized?.status==='final'&&finalized?.documentId===documentId&&finalized?.documentVersionId===versionId,'real_pdf_render_proof_finalized_exact_draft');const {data:finalDraft,error:finalErr}=await admin.from('document_drafts').select('status,final_document_id,final_document_version_id').eq('id',draftId).single();if(finalErr)throw finalErr;assert(finalDraft.status==='final'&&finalDraft.final_document_id===documentId&&finalDraft.final_document_version_id===versionId,'final_authority_persisted_exact_vault_output');
 record('phase10_3_real_cloud_arabic_pdf_boundary_passed');
}catch(e){fatal=e;console.error(e)}finally{await cleanup();if(evidence.cleanup.some(x=>!x.passed)&&!fatal)fatal=new Error('Zero-residue cleanup failed');await persist()}
if(fatal)process.exit(1);
